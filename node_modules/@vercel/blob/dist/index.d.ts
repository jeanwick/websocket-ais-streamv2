import { Readable } from 'stream';

interface PutBlobResult {
    url: string;
}
interface BlobCommandOptions {
    token: string;
}
interface PutCommandOptions extends BlobCommandOptions {
    contentType?: string;
    contentEncoding?: string;
    cacheControl?: string;
}
declare function put(pathname: string, content: string | ArrayBuffer | Blob | Readable | ReadableStream, putOptions?: PutCommandOptions): Promise<PutBlobResult>;
declare function remove(blobUrl: string, delOptions: BlobCommandOptions): Promise<boolean>;

export { PutBlobResult, put, remove };
